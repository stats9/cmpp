% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RCode.r
\name{estimate_parameters}
\alias{estimate_parameters}
\title{Estimate Parameters for Parametric CIF Using Optim}
\usage{
estimate_parameters(initial_params = rep(0.01, 4))
}
\arguments{
\item{initial_params}{A numeric vector of initial guesses for the model parameters. The default is a vector of 0.01 for each parameter.}
}
\value{
A list containing the optimization result, including the estimated parameters and other details.
}
\description{
This function uses the `optim` function to estimate the parameters of the model by minimizing the negative log-likelihood. 
Before using this function, ensure that the data is initialized with the `Initialize` function.
}
\examples{
# Example of estimating parameters using optim
features <- matrix(rnorm(100), ncol = 5)
x <- rnorm(20)
delta1 <- sample(0:1, 20, replace = TRUE)
delta2 <- sample(0:1, 20, replace = TRUE)
Initialize(features, x, delta1, delta2, h = 1e-5)
optim_result <- estimate_parameters(c(0.1, 0.2, 0.3, 0.4))
optim_result
}
